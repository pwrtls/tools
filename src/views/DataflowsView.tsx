import React, { useEffect } from 'react';

const DataflowsView: React.FC = () => {
    const loadData = () => {
        // Implementation of loadData function
    };

    useEffect(() => {
        // Initial load of dataflows and users
        loadData();
    }, [loadData]);

    return (
        // Rest of the component code
    );
};

export default DataflowsView; 